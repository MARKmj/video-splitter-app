# 🚀 GitHub Actions 错误修复 - 快速指南

## ❌ 错误信息

```
This request has been automatically failed because it uses a deprecated version of
`actions/upload-artifact: v3`
```

## ✅ 已修复

我已经更新了配置文件并提交到本地 Git。修复内容：
- `actions/checkout@v3` → `v4`
- `actions/setup-node@v3` → `v4`
- `actions/upload-artifact@v3` → `v4`

## 🎯 你需要做的（3 步）

### 步骤 1：推送修复到 GitHub

由于网络连接问题，请选择以下任一方法：

#### 方法 A：直接在 GitHub 网页编辑（最简单）

1. 访问：https://github.com/MARKmj/video-splitter-app
2. 点击文件：`.github/workflows/build.yml`
3. 点击铅笔图标（编辑）
4. 找到这几行并修改：

```yaml
# 第 18 行：改这个
uses: actions/checkout@v4

# 第 21 行：改这个
uses: actions/setup-node@v4

# 第 37 行：改这个
uses: actions/upload-artifact@v4
  with:
    name: ${{ matrix.os }}-build
    path: dist/*
    retention-days: 30
    compression-level: 6
```

5. 滚动到底部，点击绿色按钮 "Commit changes"

#### 方法 B：使用 Git 命令（如果能连接）

```bash
cd video-splitter-app
git push origin main
```

### 步骤 2：删除失败的标签和 Release

1. 访问：https://github.com/MARKmj/video-splitter-app/releases
2. 找到 v1.0.0 Release（如果有），点击 "Delete release"
3. 访问：https://github.com/MARKmj/video-splitter-app/tags
4. 找到 v1.0.0 tag，点击 "Delete tag"

### 步骤 3：重新触发构建

#### 选项 A：使用 Git 命令（推荐）

```bash
cd video-splitter-app

# 重新创建标签
git tag v1.0.0

# 推送标签（触发构建）
git push origin v1.0.0
```

#### 选项 B：在 GitHub 网页手动触发

1. 访问：https://github.com/MARKmj/video-splitter-app/actions
2. 点击左侧 "Build and Release"
3. 点击右侧 "Run workflow" 按钮
4. 选择 "main" 分支
5. 点击 "Run workflow" 按钮

## ⏱️ 等待构建

构建需要 5-10 分钟。查看进度：

访问：https://github.com/MARKmj/video-splitter-app/actions

你会看到两个任务：
- ✅ build (windows-latest) - 生成 Windows exe
- ✅ build (macos-latest) - 生成 Mac dmg

## 📦 下载成果

构建完成后：

1. 访问：https://github.com/MARKmj/video-splitter-app/releases
2. 找到 "v1.0.0" 版本
3. 下载：
   - `视频分段工具 Setup 1.0.0.exe` - Windows 安装程序
   - `视频分段工具-1.0.0.dmg` - Mac 安装程序

## 🎉 完成！

现在你有了一个功能完整的 Windows exe 安装包！

---

**需要详细说明？** 查看 [GITHUB_ACTIONS_FIX.md](GITHUB_ACTIONS_FIX.md)

**遇到问题？** 检查：
- 网络连接是否正常
- GitHub 仓库权限是否正确
- Actions 日志中的具体错误信息

💡 **提示**：推荐使用方法 A（在 GitHub 网页直接编辑），最简单可靠！
