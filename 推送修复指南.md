# 🌐 网络问题解决方案

## ❌ 当前问题

```
Failed to connect to github.com port 443 after 75018 ms: Couldn't connect to server
```

无法连接到 GitHub，可能原因：
- 网络防火墙限制
- 需要配置代理
- DNS 解析问题

## 🚀 解决方案（3 种方法）

### 方法 1：在 GitHub 网页直接编辑（⭐ 最简单，推荐）

**不需要命令行，直接在浏览器操作！**

#### 步骤：

1. **打开配置文件**
   - 访问：https://github.com/MARKmj/video-splitter-app
   - 点击文件：`.github/workflows/build.yml`

2. **编辑文件**
   - 点击右上角的铅笔图标 ✏️

3. **修改这 3 个地方**：

   **位置 1 - 第 18 行左右：**
   ```yaml
   # 找到这一行：
   - uses: actions/checkout@v3

   # 改成：
   - uses: actions/checkout@v4
   ```

   **位置 2 - 第 21 行左右：**
   ```yaml
   # 找到这一行：
   - uses: actions/setup-node@v3

   # 改成：
   - uses: actions/setup-node@v4
   ```

   **位置 3 - 第 37 行左右：**
   ```yaml
   # 找到这一行：
   - uses: actions/upload-artifact@v3
     with:
       name: ${{ matrix.os }}-build
       path: dist/*
       retention-days: 30

   # 改成：
   - uses: actions/upload-artifact@v4
     with:
       name: ${{ matrix.os }}-build
       path: dist/*
       retention-days: 30
       compression-level: 6
   ```

4. **提交更改**
   - 滚动到页面底部
   - 在 "Commit changes" 对话框中：
     - Commit message: `fix: update actions to v4`
     - 选择 "Commit directly to the main branch"
   - 点击绿色按钮 "Commit changes"

5. **删除失败的 Release 和 Tag**
   - 访问：https://github.com/MARKmj/video-splitter-app/releases
   - 如果有 v1.0.0 Release，点击删除
   - 访问：https://github.com/MARKmj/video-splitter-app/tags
   - 如果有 v1.0.0 tag，点击删除

6. **重新触发构建**
   - 选项 A（使用 Git）：
     ```bash
     cd video-splitter-app
     git tag v1.0.0
     git push origin v1.0.0
     ```

   - 选项 B（网页手动）：
     - 访问：https://github.com/MARKmj/video-splitter-app/actions
     - 点击 "Build and Release"
     - 点击 "Run workflow" 按钮
     - 选择 "main" 分支，点击运行

---

### 方法 2：配置代理后推送

如果你有代理（例如：127.0.0.1:7890）：

```bash
cd video-splitter-app

# 设置代理
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 推送代码
git push origin main

# 推送标签
git tag v1.0.0
git push origin v1.0.0

# 成功后取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

---

### 方法 3：使用 SSH 代替 HTTPS

```bash
cd video-splitter-app

# 切换到 SSH
git remote set-url origin git@github.com:MARKmj/video-splitter-app.git

# 推送代码
git push origin main

# 推送标签
git tag v1.0.0
git push origin v1.0.0
```

**注意**：需要先配置 SSH 密钥：
https://docs.github.com/zh/authentication/connecting-to-github-with-ssh

---

## ✅ 推荐操作流程

**最简单的方式（方法 1）：**

1. 在浏览器打开 GitHub
2. 编辑 `.github/workflows/build.yml` 文件
3. 把 3 个 `@v3` 改成 `@v4`
4. 添加 `compression-level: 6`
5. 点击 "Commit changes"
6. 删除旧的 Release 和 Tag
7. 在 Actions 页面点击 "Run workflow"

**总耗时：约 5 分钟** ⏱️

---

## 🎯 验证修复成功

完成后，访问：https://github.com/MARKmj/video-splitter-app/actions

如果看到绿色的 ✓，说明成功了！

5-10 分钟后，访问：https://github.com/MARKmj/video-splitter-app/releases

就能下载 Windows exe 了！

---

**需要帮助？** 查看你当前打开的 `build.yml` 文件，找到 `@v3` 的地方并改成 `@v4` 即可！
